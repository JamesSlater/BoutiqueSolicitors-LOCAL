// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
    apiKey: "AIzaSyBW50GJoDQVamIAnlcbKSOgMZ87uxZUdKE",
    authDomain: "boutique-solicitors.firebaseapp.com",
    databaseURL: "https://boutique-solicitors.firebaseio.com",
    projectId: "boutique-solicitors",
    storageBucket: "boutique-solicitors.appspot.com",
    messagingSenderId: "693428903171",
    appId: "1:693428903171:web:cb18b8984508c45e"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
